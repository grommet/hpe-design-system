# TimeInput Review TODOs

## Inferred fields — verify before merging

The following fields were inferred because no legacy TimeInput MDX documentation exists:

- `usage.whenToUse`: Verify the task-oriented scenarios against the Grommet API and intended HPE usage.
- `anatomy`: Verify the input field, time sections, and optional meridiem section against the rendered component and Figma design.
- `dosAndDonts`: Verify the time-format and component-selection guidance against the component API and design guidance.

Review these fields against the Grommet source and Figma file before merging.
