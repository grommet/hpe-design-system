# Example environment configuration for PocketBase deployment
# Copy this file to .env and update with your project values

# Google Cloud Project ID (required)
GOOGLE_CLOUD_PROJECT=your-project-id

# Deployment region (optional, defaults to us-central1)
REGION=us-central1

# Cloud Storage bucket name (optional, defaults to ${PROJECT_ID}-pocketbase-data)
BUCKET_NAME=your-bucket-name

# Service account name (optional, defaults to pocketbase-service-account)
SERVICE_ACCOUNT_NAME=pocketbase-service-account

# Cloud Run service name (optional, defaults to pocketbase-app)
SERVICE_NAME=pocketbase-app
