import { EmptyState } from 'aries-core'
import { InProgress } from '@hpe-design/icons-grommet'

<TshirtEmptyState />

export const TshirtEmptyState = () => (
  <EmptyState
    level={2}
    title="Update in progress"
    description="This page is temporarily unavailable as we prepare the
    updated version for our v2 Landmark theme."
    icon={<InProgress size="xxlarge" />}
  />
)