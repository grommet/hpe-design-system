import { Box, Image } from 'grommet';
import { Example, BestPracticeGroup } from '../../layouts';
import { ButtonSizingExample, SpacingBestPractices } from '../../examples';

## What is t-shirt sizing?

T-shirt sizing is a tokenized scale that maps familiar clothing sizes (`small`, `medium`, `large`, etc.) to specific pixel values for sizing and spacing.

Prefer t‑shirt size tokens over hard pixel values. If a new value is needed, report it so it can be added as a token.

## Benefits

- Creates consistent interfaces across applications
- Establishes common language between designers and developers
- Enables responsive scaling across screen sizes
- Maintains composability and scalability

## The scale

The sizing scale follows three core principles:

- **Proportional**: Each step maintains a natural ratio to other steps
- **Composable**: Each step divides into halves, thirds, quarters; combines from smaller steps
- **Flexible**: Subsets serve various use cases from dense to spacious layouts

Built from dual base units: `24px` (primary) and `16px` (supplementary). The primary and supplementary scales combine via doubling/halving to produce:

**Pixel progression:** 1, 2, 3, 4, 6, 8, 12, 16, 24, 32, 48, 64, 96, 128, 192, 256, 384, 512, 768, 1024, 1536

See [Layout and Spacing design tokens](/design-tokens/layout-and-spacing) for precise pixel values, implementation and versioning details.

## Composability

Sizes maintain mathematical relationships for predictable combinations.

```jsx
// Spacing sizes double:
xsmall (12px) + xsmall (12px) = medium (24px)
medium (24px) + medium (24px) = xlarge (48px)

// Container sizes follow the same pattern:
xsmall (192px) + xsmall (192px) = medium (384px)
medium (384px) + medium (384px) = xlarge (768px)

// Intermediate sizes fill gaps:
small (16px) = medium (24px) - xxsmall (8px)
large (32px) = medium (24px) + xxsmall (8px)
```

## Component Dimensions

Control component size using:
- `width` and `height` props ([Box](/components/box))
- `size` prop ([Avatar](https://v2.grommet.io/avatar?theme=hpe), [DataTable](/components/datatable), [Text](/foundation/typography#text))
- `row` and `column` dimensions ([Grid](/components/grid))

Refer to [Grommet documentation](https://v2.grommet.io/) for component-specific sizing props.

```
<Box width="medium" height="small" />
```

<Box width="large">
  <Image src="/box-sizing.png" alt="Demonstration of t-shirt sizing on Box" />
</Box>

## Spacing

Control spacing using t-shirt sizes for `pad`, `gap`, `margin`, and `border`.

- Spacing (`edgeSize`): Controls pad, gap, and margin
- Border (`borderSize`): Controls border width

```
<Box
    width="medium"
    height="small"
    pad="large"
/>
```

<Box width="medium" margin={{bottom: 'large'}}>
  <Image
    src="/edge-sizing.png"
    alt="Demonstration of t-shirt sizing for pad on Box"
  />
</Box>

### Spacing best practices

<BestPracticeGroup>
  <Example
    height="none"
    bestPractice={{
      type: 'do',
      message:
        'Use spacing design tokens to style compositional or layout components like cards, layers, and grid spacing.',
    }}
    width="100%"
  >
    <SpacingBestPractices />
  </Example>
  <Example
    height="none"
    bestPractice={{
      type: 'dont',
      message: `Use spacing design tokens to style element-level components. Instead use component-specific tokens or element tokens which will ensure correct sizing of elements across screen sizes.`,
    }}
  >
    <SpacingBestPractices bestPractice={false} />
  </Example>
</BestPracticeGroup>

## Borders

Use named border size tokens to keep borders aligned with the scale. Avoid arbitrary pixel widths; apply them via the border prop, e.g. `border={{ size: 'small' }}`.

```
<Box 
  border={{ color: 'border', size: 'small' }}
  width="medium"
  height="small" 
/>
```

<Box width="medium" margin={{bottom: 'large'}}>
  <Image
    src="/border-sizing.png"
    alt="Demonstration of t-shirt sizing for border on Box"
  />
</Box>

## Containers

Use container size tokens for width and height constraints to preserve responsive flow. Avoid over‑constraining; prefer [content-driven layouts](/templates/content-layouts#content-driven-layouts).

## Radius

Apply larger radius tokens to high‑level sections and smaller radii to inner elements to maintain visual hierarchy.

## Default Size

Treat `medium` as the implicit size for components with a `size` prop; omit `size="medium"` when using the default to reduce noise.

<Example
  docs="https://v2.grommet.io/button?theme=hpe#props"
  code="https://raw.githubusercontent.com/grommet/hpe-design-system/master/aries-site/src/examples/components/button/ButtonSizingExample.js"
  height={{ min: 'small' }}
>
  <ButtonSizingExample />
</Example>

## Responsiveness

Layouts should be designed with responsiveness in mind to meet a user wherever they are &mdash; whether on a desktop, tablet, or mobile device.

The values of semantic spacing and radius design tokens step down in size at the `small` [breakpoint](/design-tokens/global#breakpoint). This retains appropriate proportions between shape, spacing, and content as the screen size decreases.

When utilizing the CSS files, a media query is included in the `dimension.small.css` file to adjust the token values at the `small` breakpoint.

To further refine layouts for different screen sizes, various [breakpoints](/design-tokens/global#breakpoint) can be used to instruct logic for layout adjustments.