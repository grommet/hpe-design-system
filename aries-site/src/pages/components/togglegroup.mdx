Use case examples (generate a shortlist of common use cases for this component and a one sentence description of why it applies to this use case). Code examples will be created as a separate scope of work.
Anatomy
Follow table format. Coded diagram will be separate scope.
Types (this should capture that it can be single or multiple selection)
Accessibility
Keyboard behavior (tab to group, use arrow keys within group)
In icon-only cases, ensure each icon has a clear a11yTitle.
