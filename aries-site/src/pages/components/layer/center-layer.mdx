import { Button, Notification } from 'grommet';
import { Example } from '../../../layouts';
import { CenterLayerAnatomy } from '../../../examples';

## Anatomy

This diagram highlights details specific to center layers. See [general layer anatomy](/components/layer#anatomy) for specs that apply to all layers.

<Example plain>
  <CenterLayerAnatomy />
</Example>

1.  **Close button** (only on layers with no footer actions): A button to close the layer.
1.  **Footer actions**: The actions should be right-aligned with primary action on the right. One action should be a "Cancel" button that closes the layer. See [layer closing behavior](/components/layer#actionable) for details.

<Notification
  status="info"
  message="A layer should either have actions in the footer or a close button in the header, not both."
  width="large"
  margin={{ bottom: 'medium' }}
/>

## Examples

### Double confirmation
